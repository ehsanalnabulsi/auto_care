import 'package:flutter/widgets.dart';

class TowCarOwnerOrders extends StatelessWidget {
  const TowCarOwnerOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
